class AppDurations {
  static const Duration expandingDuration = Duration(milliseconds: 350);
  static const Duration debounceDuration = Duration(milliseconds: 500);
  static const Duration oneDay = Duration(days: 1);
  static const Duration oneMinute = Duration(minutes: 1);
  static const Duration week = Duration(days: DateTime.daysPerWeek);
  static const Duration buttonAnimationDuration = Duration(milliseconds: 200);
  static const Duration menuAnimationDuration = Duration(milliseconds: 500);
  static const Duration oneSecond = Duration(seconds: 1);
  static const Duration dialogDelay = Duration(seconds: 3);
  static const Duration legendAnimationDuration = Duration(milliseconds: 500);
}
